fastapi==0.141.1  # FASE 5 Sprint 4 — upgrade dari 0.116.1 agar starlette 0.49+ kompatibel
uvicorn[standard]==0.35.0
sqlalchemy==2.0.35
pydantic==2.12.0
pydantic-settings==2.15.0
# FASE 5 Sprint 6 — migrate dari python-jose ke PyJWT[crypto].
# PyJWT lebih aktif maintained, tidak depend pada ecdsa library
# (yang punya PYSEC-2026-1325 Minerva timing attack, no-fix).
# FLIPUS pakai HS256 only — PyJWT support penuh via cryptography.
pyjwt[crypto]==2.13.0
passlib[bcrypt]==1.7.4
# FASE 5 Sprint 8 — pin bcrypt==4.0.1: passlib 1.7.4 incompatible dgn bcrypt 4.1+,
# yang menghapus bcrypt.__about__.__version__ (warning trapped saat login).
bcrypt==4.0.1

# FASE 5 Sprint 4 — upgrade deps untuk menutup pip-audit vulns.
# cryptography 50.0.1 menutup 6 advisories (44.0.3 rentan).
cryptography==50.0.1
# Pillow 12.3.0 menutup 14 advisories (11.1.0 rentan, utamanya image-processing CVE).
Pillow==12.3.0
# python-multipart 0.0.32 menutup 7 advisories (0.0.12 rentan, multipart-parser DoS).
python-multipart==0.0.32
# Starlette 0.52.1 menutup 1 advisory (PYSEC-2026-1942). 5 advisories lain
# butuh starlette 1.0+ yang belum kompatibel dengan prometheus-fastapi-instrumentator.
# Di-suppress via `pip-audit --ignore-vuln` di Makefile + ci.yml — lihat rationale
# di FASE5_SPRINT4_SUMMARY.md §A2.
starlette>=0.52.1,<1.0  # transitif via fastapi>=0.123.0
# ecdsa: REMOVED FASE 5 Sprint 6 — transitif via python-jose yang sudah di-uninstall.
# ecdsa 0.19.2 punya PYSEC-2026-1325 (no-fix Minerva timing attack).
# Mitigasi: migrate python-jose → PyJWT (security.py). HS256 JWT tidak pernah
# invoke ecdsa code path.

apscheduler==3.10.4
httpx==0.28.1
ollama==0.4.7
google-generativeai==0.8.5
reportlab==4.2.5
openpyxl==3.1.5
pyotp==2.9.0
qrcode==7.4.2

# FASE 3 Sprint 1: Rate limiting + file validation
slowapi==0.1.9
filetype==1.2.0
limits[redis]==3.13.0

# FASE 3 Sprint 4 — Developer tooling
mypy==1.13.0
pytest-cov==5.0.0
ruff==0.7.4

# FASE 5 Sprint 2 — Prometheus metrics
prometheus_client==0.21.1
prometheus-fastapi-instrumentator==7.0.0

# FASE 5 Sprint 3 — Security tooling (used in CI)
# bandit: AST-based security linter for common Python vulns (eval,
#         subprocess shell=True, weak hash, etc.). Pure offline.
bandit==1.7.10
# pip-audit: official PyPA tool. Audits installed deps against the OSV /
#            PyPI Advisory DB. Works offline after first download of the
#            vulnerability snapshot. Replaces safety (which went
#            commercial in v3 and now requires an API key).
pip-audit==2.7.3
